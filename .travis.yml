language: csharp
mono: none
dotnet: 2.2.0
solution: Todo.sln
install:
  - nuget restore Todo.sln
  - nuget install xunit.runners -Version 1.9.2 -OutputDirectory testrunner
script:
  - dotnet restore
  - dotnet test